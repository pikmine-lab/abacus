<!-- The project instructions live in AGENTS.md, so every coding agent reads the
     same file. Claude Code reads CLAUDE.md and not AGENTS.md, which makes this
     import the bridge between the two. Keep this file a single import line:
     content belongs in AGENTS.md. Block-level HTML comments are stripped before
     a CLAUDE.md reaches the model, so this note costs no context. -->
@AGENTS.md
